{% extends 'NSAdminBundle:Section:settings.html.twig' %}

{% block content_header %}
    <h3>Типы данных</h3>
{% endblock %}

{% block content_content %}
    <a href="{{ url('ns_admin_bundle', { adminBundle: 'NSCatalogBundle', adminController: 'types', adminAction: 'form'}) }}" class="btn">Добавить тип данных</a>

    {% if types %}
        <table class="table table-striped table-hover">
            <thead>
            <tr>
                <th>Название</th>
                <th style="width:100px">Операции</th>
            </tr>
            </thead>
            <tbody>
            {% for type in types %}
                <tr>
                    <td>{{ type.title }}</td>
                    <td>
                        <div class="btn-group">
                            <a href="{{ url('ns_admin_bundle', { adminBundle: 'NSCatalogBundle', adminController: 'types', adminAction: 'form'}) }}?id={{ type.id }}" class="btn btn-mini"><i class="icon-pencil"></i></a>
                            <a href="{{ url('ns_admin_bundle', { adminBundle: 'NSCatalogBundle', adminController: 'types', adminAction: 'delete'}) }}?id={{ type.id }}" class="btn btn-mini ns-confirm"><i class="icon-trash"></i></a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}
{% endblock %}