{% extends 'NSAdminBundle::layout.html.twig' %}

{% block content %}
	<h3>Управление товарами</h3>

	<div class="row">
		<div class="span3">
			{{ render(controller('NSCatalogBundle:AdminCategories:categoryTree'))  }}
		</div>
		<div class="span9">
			<div>
				<a href="{{ url('ns_admin_bundle', { adminBundle: 'NSCatalogBundle', adminController: 'items', adminAction: 'form' }) }}" class="btn">Добавить товар</a>
			</div>

		</div>
	</div>
{% endblock %}
